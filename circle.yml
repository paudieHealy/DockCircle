
#For Docker Hub
#deployment:
  #dockerhub:
    #branch: master
    #commands:
      #- $docker_trigger
deployment:
  prod:
    branch: master
    heroku:
      appname: herokuappdeploy

test:
  override:    
    - mocha