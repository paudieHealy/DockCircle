
#For Docker Hub
#deployment:
  #dockerhub:
    #branch: master
    #commands:
      #- $docker_trigger
deployment:
  prod:
    branch: master
    heroku:
      appname: DockCircle

test:
  override:    
    - mocha